transport_options <- function() {
  return(c("exact", "sinkhorn", "greenkhorn",
           "randkhorn", "gandkhorn",
           "hilbert", "rank",
           "univariate.approximation.pwr",
           "swapping"))
}